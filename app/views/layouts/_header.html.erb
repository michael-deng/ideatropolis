<nav class="navbar navbar-default">
  <div class="container-fluid">
    <ul class="nav navbar-nav navbar-left">
      <li><%= link_to "Unicornbase", root_path %></li>
    </ul>
    <ul class="nav navbar-nav navbar-center">

    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li class="searchbar">
        <%= form_tag ideas_path, :method => :get do %>
          <% text_field_tag :search, params[:search], placeholder: "Search for ideas" %>
        <% end %>
      </li>
      <li><%= link_to "Discover", categories_path %></li>
      <% if user_signed_in? %>
        <li><%= link_to "Post", new_idea_path %></li>
        <li class="dropdown">
          <a href="" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            <i class="fa fa-bell fa-lg"></i>
          </a>
          <ul class="dropdown-menu" role="menu">
            <% @notifications = current_user.notifications.last(8) %>
            <%= render partial: "notifications/notification", collection: @notifications %>
            <li class="dropdown-caption"><%= link_to "See all notifications", notifications_path %></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="" class="dropdown-toggle profile" data-toggle="dropdown" role="button" aria-expanded="false">
            <%= image_tag(current_user.avatar.url(:icon)) %>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Profile", user_path(current_user) %></li>
            <li><%= link_to "Interests", edit_category_registration_path(current_user) %></li>
            <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>        
      <% else %>
        <li><%= link_to "Sign in", new_user_session_path %></li>
      <% end %>
	  </ul>
  </div>
</nav>